// Directly remove the photo element
$('#photo_<%= @photo.id %>').remove();

// Hide the photo check element if there are no photos
<% if @character.photos.blank? %>
  $('#photo_check').hide();
<% end %>